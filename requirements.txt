# Core Framework (SECURITY UPDATED)
fastapi>=0.115.0      # Security patches for CVE-2024-24762, CVE-2024-23917
uvicorn[standard]>=0.30.0 # Performance + HTTP/2 support, security updates

# Data Validation and Settings (COMPATIBILITY UPDATED)
pydantic>=2.8.0         # Latest stable with performance improvements
pydantic-settings>=2.3.0 # Compatibility fix for pydantic 2.8+

# Configuration and HTTP (SECURITY UPDATED)
PyYAML>=6.0.2         # Security patch for CVE-2024-35327
httpx[http2]>=0.27.0  # Performance + HTTP/2 support, connection pooling fixes

# Performance Optimizations (NEW)
uvloop>=0.19.0; sys_platform != "win32"  # 2-3x asyncio performance boost (Linux/macOS only)
orjson>=3.10.0        # 2-5x JSON serialization performance

# API Features
slowapi>=0.1.9        # For rate limiting API requests
anyio>=3.7.1          # Asynchronous networking library
PyJWT>=2.8.0          # For JSON Web Tokens authentication
pyasn1>=0.6.1         # Dependency of PyJWT

# Testing
pytest>=8.4.2         # Test framework
pytest-asyncio>=0.21.0 # Async testing support

# Web UI (SECURITY UPDATED)
Flask>=3.0.0          # Security updates, modern features
python-dotenv>=0.19   # Environment file management

# Development Tools (NEW)
black>=24.0.0         # Code formatting
isort>=5.13.0         # Import sorting
mypy>=1.9.0           # Type checking
ruff>=0.5.0           # Fast linting
